# Random-Data-Simulator
Generates random "sales" data (numbers with random), saves to file, reads back, sorts, computes mean (statistics), and uses match/conditionals for categories. Handles exceptions and loops. Data science vibe: simulating datasets for analysis
